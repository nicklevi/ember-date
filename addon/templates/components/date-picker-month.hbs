{{#if showWeekdays}}
  <div class='date-picker__weekdays'>
    {{#each weekdays as |day|}}
      <div class='date-picker__weekday'>
        {{day}}
      </div>
    {{/each}}
  </div>
{{/if}}

<div class='date-picker__days'>
  {{#each daysInMonth as |date|}}
    {{#if date}}
      <button
        data-test="day-{{date.month}}-{{date.day}}"
        disabled={{date.disabled}}
        class="date.disabled:date-picker__day--disabled date.inRange:date-picker__day--in-range date.isToday:date-picker__day--today date.isSelected:date-picker__day--selected"
        {{action 'selectDate' date.date}}>
        {{date.day}}
      </button>
    {{else}}
      <div class="date-picker__day__placeholder"></div>
    {{/if}}
  {{/each}}
</div>